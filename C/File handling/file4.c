#include <stdio.h>

int main()
{
    FILE *filePtr = NULL;

    // 1.
    //     filePtr = fopen("file4.txt", "r");

    //     // char c = fgetc(filePtr);
    //     // printf("%c\n", c);
    //     char s[100];
    //     fgets(s, 100, filePtr);
    //     printf("%s\n", s);

    // 2.
    // filePtr = fopen("file4.txt", "w");

    // putc('H', filePtr);
    // fputs("\nHello my name is HY\n",filePtr);
   
    // 3.
    // filePtr = fopen("file4.txt", "a");

    // putc('H', filePtr);
    // fputs("\n1Hello my name is HY\n",filePtr);

        
    return 0;
}